import { style } from 'snabbdom-material'

export default {
  init: () => {
    style.init(style.getComponentVariables({

      sidenavItemSelectedColor: '#c2009b'

    }), {

      // optionally set style overrides here

    })
  }
}

